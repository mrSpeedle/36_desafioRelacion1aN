<p style="color: green"><%= notice %></p>

<div class="container">
  <div class="row justify-content-center">
    <%= render @movie, show_links: false %> <!-- FALSE PA ESCONDERLOS COMO LO HACÍA TU EX -->
  </div>

  <div class="text-center">
    <%= link_to "Edit this movie", edit_movie_path(@movie), class: "btn btn-secondary" %> |
    <%= link_to "Back to movies", movies_path, class: "btn btn-secondary" %>
    <%= button_to "Destroy this movie", @movie, method: :delete, class: "btn btn-danger mt-2" %>
  </div>
</div>
